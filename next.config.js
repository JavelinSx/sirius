/** @type {import('next').NextConfig} */
const nextConfig = {
    output: 'export', // Экспорт статического сайта
    basePath: '/', // Замените 'sirius' на имя вашего репозитория
    assetPrefix: '/', // Замените 'sirius' на имя вашего репозитория
}

module.exports = nextConfig;